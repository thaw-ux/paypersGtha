# บันทึกบทสนทนาและการพัฒนาระบบ Paypers AI (Self-Hosted)
**วันที่:** 30 สิงหาคม 2569 (30/08/2026)  
**ชื่อไฟล์:** `conver1_300869.md`  
**โปรเจกต์:** Paypers AI Clone (GitHub: `paypersG`)

---

## 📌 1. เป้าหมายและความต้องการของโปรเจกต์ (Requirements)
- ศึกษาโครงสร้างและแนวทางการทำงานของ [Paypers.ai](https://paypers.ai)
- พัฒนาระบบจัดการใบเสร็จ สลิปโอนเงิน และเอกสารการเงินอัตโนมัติสำหรับใช้งานส่วนตัว/ธุรกิจ
- **ที่เก็บข้อมูล & ฐานข้อมูล:**
  - **Google Drive:** ใช้เป็น Storage จัดเก็บรูปภาพใบเสร็จ สลิปโอนเงิน และไฟล์ PDF แยกโฟลเดอร์ตามปี/เดือนอัตโนมัติ
  - **Google Sheets:** ใช้เป็น Database สำหรับบันทึกข้อมูลธุรกรรมแบบเรียลไทม์ (21 คอลัมน์มาตรฐาน)
- **ช่องทางการเข้าถึง 2 ระบบ (Dual Interfaces):**
  1. **LINE OA (LINE Messaging API):** ส่งรูปบิล/สลิป หรือพิมพ์ข้อความในแชท ระบบจะอ่านข้อมูล บันทึกรูปเข้า Google Drive, เขียนข้อมูลลง Google Sheets และตอบกลับด้วย LINE Flex Message
  2. **Web Application Dashboard (Next.js + Tailwind CSS):** หน้าเว็บสำหรับดูภาพรวมการเงิน (Overview Analytics), ตารางจัดการรายการธุรกรรมพร้อมแก้ไขและพรีวิวรูปจาก Drive (Transactions & Split Viewer), หน้าอัปโหลดสแกนบิลผ่านเว็บพร้อม Live AI Preview, รายงานสรุปภาษีซื้อ/หัก ณ ที่จ่าย (Tax Summary) และหน้าตั้งค่าระบบ
- **AI Engine:** Google Gemini API (Multimodal OCR ภาษาไทย สกัดชื่อร้านค้า, ยอดเงิน, VAT 7%, เลขประจำตัวผู้เสียภาษี 13 หลัก, หัก ณ ที่จ่าย, รายการสินค้า)

---

## 🏗️ 2. สถาปัตยกรรมและโครงสร้างไฟล์ที่พัฒนา

```
d:/paypers/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   ├── line/webhook/route.ts        # LINE Webhook Receiver (HMAC SHA-256 Signature Verify)
│   │   │   ├── transactions/route.ts        # CRUD Transactions (Google Sheets Database)
│   │   │   ├── transactions/[id]/route.ts   # Update / Delete Transaction by ID
│   │   │   ├── scan/route.ts                # Gemini Multimodal OCR + Drive Upload API
│   │   │   ├── drive/view/[fileId]/route.ts # Secure Image Streaming Proxy from Google Drive
│   │   │   ├── summary/route.ts             # Financial Stats & Analytics API
│   │   │   └── setup/route.ts               # Health Checks & One-Click DB Init
│   │   ├── dashboard/page.tsx               # Overview Dashboard (KPIs, Charts, Recent Records)
│   │   ├── transactions/page.tsx            # Full Transactions Management Page
│   │   ├── scan/page.tsx                    # AI Live OCR Drag-and-Drop Scanner Page
│   │   ├── tax-reports/page.tsx             # Input VAT (ภ.พ.30) & WHT Reports
│   │   ├── settings/page.tsx                # Diagnostics, Health Status & Setup Guide
│   │   ├── layout.tsx                       # Root Shell with Sidebar & Header
│   │   ├── page.tsx                         # Redirect to /dashboard
│   │   └── globals.css                      # Modern Typography & Glassmorphism UI
│   ├── components/
│   │   ├── layout/Sidebar.tsx               # Navigation Sidebar with Sync Status
│   │   ├── layout/Header.tsx                # Header Bar with Thai Date & Quick Add
│   │   ├── dashboard/MetricCards.tsx        # Financial Summary Cards (Expense, Income, VAT, WHT)
│   │   ├── dashboard/AnalyticsCharts.tsx    # Recharts Monthly Trends & Category Donut Charts
│   │   ├── transactions/TransactionTable.tsx# Filterable & Searchable Grid + CSV Exporter
│   │   ├── transactions/ReceiptModal.tsx    # Side-by-side Split View (Drive Photo + Sheet Editor)
│   │   ├── scan/DropzoneScanner.tsx         # Drag-and-drop live OCR analyzer
│   │   └── tax/TaxReportView.tsx            # Tax Tables, VAT calculation & Print Formatter
│   ├── lib/
│   │   ├── google/
│   │   │   ├── auth.ts                      # Google Service Account JWT Authenticator
│   │   │   ├── drive.service.ts             # Auto-folder (Expenses/YYYY/MM) & Buffer Upload
│   │   │   └── sheets.service.ts            # Canonical Column Mapper & Sheet CRUD Operations
│   │   ├── gemini/
│   │   │   ├── client.ts                    # Google Generative AI Singleton
│   │   │   ├── prompts.ts                   # Thai Receipts, Slips & Text Prompts
│   │   │   └── parser.service.ts            # Multimodal Image OCR & JSON Extractor
│   │   ├── line/
│   │   │   ├── client.ts                    # LINE Bot Client & Content Downloader
│   │   │   ├── flex-templates.ts            # Interactive LINE Flex Message Builders
│   │   │   └── handler.ts                   # LINE Image / Text Event Processor
│   │   ├── utils.ts                         # Currency (฿), Thai Date & ID Helper
│   │   └── types/transaction.ts             # Type Definitions & JSON Schemas
├── .env.example                             # Environment Configuration Template
├── package.json                             # Dependencies & Scripts
├── tailwind.config.ts                       # Brand Styling & Color Tokens
└── README.md                                # คู่มือการติดตั้งและคำแนะนำ
```

---

## 📊 3. โครงสร้างข้อมูลใน Google Sheets (21 Canonical Columns)

| คอลัมน์ | ชื่อฟิลด์ | รายละเอียด |
| :--- | :--- | :--- |
| A | `ID` | รหัสธุรกรรม (UUID หรือ TXN-YYYYMMDD-XXXX) |
| B | `Timestamp` | เวลาที่บันทึกข้อมูลเข้าระบบ (ISO) |
| C | `Date` | วันที่ตามเอกสาร (YYYY-MM-DD) |
| D | `Time` | เวลาตามเอกสาร (HH:mm:ss) |
| E | `Type` | `expense` (รายจ่าย) หรือ `income` (รายรับ) |
| F | `Category` | หมวดหมู่ (ค่าเดินทาง, ค่าอาหาร, ค่าซอฟต์แวร์ ฯลฯ) |
| G | `Merchant` | ชื่อร้านค้า / บริษัทคู่ค้า |
| H | `Tax_ID` | เลขประจำตัวผู้เสียภาษี 13 หลัก |
| I | `Subtotal` | ยอดมูลค่าก่อนภาษี |
| J | `VAT` | ภาษีมูลค่าเพิ่ม 7% |
| K | `WHT` | ภาษีหัก ณ ที่จ่าย (1%, 3%, 5%) |
| L | `Grand_Total` | ยอดรวมสุทธิ |
| M | `Payment_Method` | ช่องทางชำระ (`bank_transfer`, `promptpay`, `cash`, `credit_card`) |
| N | `Document_Type` | ชนิดเอกสาร (`tax_invoice`, `receipt`, `bank_slip`, `text_note`) |
| O | `Items_JSON` | รายการสินค้า/บริการย่อยในบิล |
| P | `Drive_File_ID` | รหัสไฟล์ใน Google Drive |
| Q | `Drive_View_URL` | ลิงก์ดูภาพ |
| R | `Source` | แหล่งที่มา (`line_oa` หรือ `web_app`) |
| S | `Created_By` | ผู้บันทึก (LINE User ID หรือ Admin) |
| T | `Status` | `completed`, `pending_review`, `deleted` |
| U | `Notes` | ข้อความสรุปจาก AI หรือหมายเหตุเพิ่มเติม |

---

## ⚙️ 4. ข้อมูลการตั้งค่า (Configuration & Credentials)

- **Service Account Email:** `servicepaypers@substation-jfai.iam.gserviceaccount.com`
- **Google Spreadsheet:** `https://docs.google.com/spreadsheets/d/168K83clc8qqouUa4okIqsRrhdCS9EE9LosX6DstJkAo/edit`  
  *(ID: `168K83clc8qqouUa4okIqsRrhdCS9EE9LosX6DstJkAo`)*
- **Google Drive Root Folder:** `https://drive.google.com/drive/folders/1fUexrKSTCNT1mMLObSaF4E6VbS-eTuLF`  
  *(ID: `1fUexrKSTCNT1mMLObSaF4E6VbS-eTuLF`)*
- **Google Cloud APIs ที่เปิดใช้งานแล้ว:**
  - ✅ **Google Sheets API:** เปิดใช้งานแล้ว
  - ✅ **Google Drive API:** เปิดใช้งานแล้ว
- **ผลการสร้างฐานข้อมูลเริ่มต้น (Database Auto-Init):**
  - สร้างแท็บตารางชื่อ `Transactions` พร้อมหัวตาราง 21 คอลัมน์ใน Google Sheet เรียบร้อย
  - สร้างโครงสร้างโฟลเดอร์ `Expenses/2026/08` ใน Google Drive เรียบร้อย

---

## 📱 5. การตั้งค่าฝั่ง LINE Official Account (LINE OA)

### 5.1 LINE Developers Console:
- **Webhook URL:** `https://<YOUR_DOMAIN>/api/line/webhook` (ต้องเป็น HTTPS)
- **Use Webhook:** เปิดใช้งาน (**Enabled**)
- **Webhook redelivery:** เปิดใช้งาน (**Enabled**)

### 5.2 LINE Official Account Manager:
- **Response mode:** เลือกเป็น **บอท (Bot)**
- **Webhook:** เลือกเป็น **เปิด (Enabled)**
- **Auto-response messages:** เลือก **ปิด (Disabled)** (เพื่อไม่ให้บอทพื้นฐานของ LINE ส่งข้อความซ้ำซ้อน)

---

## 🔒 6. การตรวจสอบความปลอดภัยบน GitHub (`paypersG`)

- **ผลการ Audit ความปลอดภัย:**
  - ✅ ไฟล์ `.env` และ `.env.local` ถูกบล็อกโดย `.gitignore` ไม่ถูกอัปโหลดขึ้น GitHub 100%
  - ✅ ไฟล์ `service-account.json` และโฟลเดอร์ `key/` ถูกบล็อกโดย `.gitignore` 100%
  - ✅ ตรวจสอบประวัติ Git ไม่พบคีย์ Secret, Token หรือ Private Key จริงใดๆ รั่วไหลขึ้น GitHub
  - ✅ มีเพียงไฟล์ `.env.example` ที่เก็บเฉพาะค่าสมมติและแม่แบบสำหรับการตั้งค่าเท่านั้น

---

## 🚀 7. การ Deploy ขึ้น Production (Vercel)

- **ปัญหาที่พบบน Netlify:** บัญชี Netlify (Team: FinancialServices) เครดิตรายเดือนเต็ม ทำให้เกิด Error `Skipped due to account credit usage exceeded`
- **การ Deploy บน Vercel สำเร็จ:**
  - **โปรเจกต์:** `paypers-gtha` (บัญชี `thaweesinnil-7066s-projects`)
  - **Production Domain:** `https://paypers-gtha.vercel.app`
  - **Web Dashboard:** `https://paypers-gtha.vercel.app/dashboard`
  - **LINE Webhook URL:** `https://paypers-gtha.vercel.app/api/line/webhook`

---

## 🛠️ 8. การแก้ไขปัญหาและข้อควรระวังในการตั้งค่า LINE Webhook

### 8.1 ปัญหา `404 Not Found`
- **สาเหตุ:** ใส่ชื่อโดเมนสมมติ หรือโดเมนที่ยังไม่ได้ Deploy จริงบน Vercel
- **วิธีแก้:** คัดลอกโดเมนจริงจากหน้า Dashboard ของ Vercel มาใส่

### 8.2 ปัญหา `308 Permanent Redirect`
- **สาเหตุ:** มีเครื่องหมายสแลชซ้ำกัน 2 ตัว เช่น `https://paypers-gtha.vercel.app//api/line/webhook` ทำให้ Next.js ส่ง 308 Redirect และ LINE Verify ไม่ผ่าน
- **วิธีแก้:** ลบให้เหลือเครื่องหมายสแลชตัวเดียว: `https://paypers-gtha.vercel.app/api/line/webhook`

### 8.3 ปัญหา `403 Forbidden` (Signature Mismatch)
- **สาเหตุ:** การตรวจสอบลายเซ็น HMAC-SHA256 ระหว่าง LINE Platform และเซิร์ฟเวอร์ไม่ตรงกัน หรือ `LINE_CHANNEL_SECRET` ใน Environment Variables ของ Vercel ยังไม่ถูกนำมาใช้ใน Build เดิม
- **การแก้ไข:**
  1. ปรับปรุงโค้ดใน `src/app/api/line/webhook/route.ts` ให้อ่าน Buffer UTF-8 และแปลง JSON Body อย่างปลอดภัย
  2. ตรวจสอบค่า `LINE_CHANNEL_SECRET` ใน Vercel Settings -> Environment Variables ให้ตรงกับ `5d061cf14cff4ac642818d1d64782f43` (ไม่มีช่องว่างเว้นวรรค)
  3. เมื่อมีการอัปเดตโค้ดหรือ Environment Variables ให้ทำการ Redeploy แล้วกดปุ่ม **Verify** ใน LINE Developers Console อีกครั้ง

---
*บันทึกโดย Antigravity AI เมื่อวันที่ 30 สิงหาคม 2569*

